#include<stdio.h>
int main(){
    int age =25;
    int num=35;//int is used for saving integers
    char name="Tariq";//char used to save characters
    float pi= 3.14;//float is used for saving decimal numbers
    //giving variable name cannot start with number or signs 
    //it can only start by '_' or any alphabet 
    //its case sensitive if you are saving A =30 and a =79 
    // both are saved seprately
    //while saving var name you can only use '_'
    //no other signs can be used !!!!
    //example below
    int a=79;//small a have different value 
    int A=9090;//capital a have different value
    age=35;//age can be updated later on 
    //as a good programmer you should alaways give the variable name 
    //that is actually saying what is saved in it
    //example int age = 25 ; it shows that age is saved here 
    return 0;
}
